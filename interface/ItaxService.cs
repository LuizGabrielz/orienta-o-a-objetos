namespace Course.Services { // Colocar o "i" na maiusculo na criação da classe
interface ITaxService { // Colocar a palavra interface ao inves de classe 
    
        double Tax(double amount);
    }
}